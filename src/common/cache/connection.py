import redis

from common import settings

# We need to specify the hostname of the redis master
# redisClient = redis.StrictRedis(host=settings['redis']['host'], port=settings['redis']['port'], db=0)
